import { NextRequest, NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabase-admin";
const EVENTS=new Set(["add_to_cart","view_cart","begin_checkout","order_created"]);
function source(req:NextRequest){const r=req.headers.get("referer")||"";if(/facebook|fb\.com/i.test(r))return"facebook";if(/instagram/i.test(r))return"instagram";if(/google/i.test(r))return"google";if(/whatsapp/i.test(r))return"whatsapp";return r?"referral":"direct";}
export async function POST(req:NextRequest){try{const b=await req.json();if(!b.storeId||!EVENTS.has(b.eventName))return NextResponse.json({error:"Evento inválido"},{status:400});const {error}=await supabaseAdmin.from("analytics_events").insert({store_id:b.storeId,visitor_id:b.visitorId||null,session_id:b.sessionId||null,event_name:b.eventName,path:b.path||null,product_id:b.productId||null,combo_id:b.comboId||null,order_id:b.orderId||null,item_name:b.itemName||null,quantity:Number(b.quantity)||null,value:Number(b.value)||null,source:source(req),campaign_source:b.source||null,campaign_medium:b.medium||null,campaign_name:b.campaign||null,campaign_content:b.content||null,metadata:b.metadata||{}});if(error)throw error;return NextResponse.json({ok:true});}catch(e){console.error("ANALYTICS EVENT",e);return NextResponse.json({error:"No registrado"},{status:500});}}
